pipeline {
    agent { 
        label 'stage' 
    }
    options {
        // test config to not keep ALL builds of PR check
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
    stages {
        /*
        stage('Set up node version') {
            steps {
                echo 'Set up node version'
                sh 'nvm use 16'
                }
            }
        */
        stage('Intall node modules') {
            steps {
                echo 'Installing modules...'
                sh 'npm i'
                }
            }
        stage('Run 1 test for example') {
            steps {
                echo 'Run one test_for example'
                sh 'npm run test DeleteStreetcodeModal'
            }
        }
        /* uncomment me when you will fix lint
        // some new comment
        stage('Run lint') {
            steps {
                echo 'Runt lint check'
                sh 'npm run lint'
            }
        }
        */
    }
}
