import './RelatedTerms.styles.scss';

import { useState } from 'react';

const RelatedTermsTable = () => {
    const [active, setActive] = useState();

    return (
        <div>TEXT</div>
    );
};

export default RelatedTermsTable;
